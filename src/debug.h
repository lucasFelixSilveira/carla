#ifndef DEBUG_H
# define DEBUG_H 
# include "vector.h"

  void pRoot (Vector *root);
  void pSmart (Vector *root, Vector *subRoot);
  void pTokens (Vector *tks);
  void pHeader (int *count, char *header);

#endif