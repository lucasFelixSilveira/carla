#include "stdio"
#include "stdheap"
#include "stdstring"

struct output {
  int8 print = ([]ascii msg) {
    []byte buffer = heap::alloc(128); 
    string::format(buffer, "The argument is: {s}", msg);
    io::println(buffer);
    heap::dump(buffer);
    return 0;
  }
} impl;

int32 main = (int32 argc, []char* argv) {
  for int32 i : 0..argc {
    char* arg = argv[i];
    output::print(arg);
  }
  return 1;
}