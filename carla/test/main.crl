#include "stdio"

struct person_t {
  int8 new = ([]byte name) {
    return 0;
  } 

  int8 say = ([]byte to_say) {
    io::println(to_say);
    return 0;
  }
} impl {
  []char name;
  uint8 age;
};

int32 main = () {
  person_t::say("Hello, world");
  return 0;
}